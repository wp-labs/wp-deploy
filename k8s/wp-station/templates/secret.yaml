apiVersion: v1
kind: Secret
metadata:
  name: {{ include "station.secretName" . }}
  labels:
    {{- include "station.labels" . | nindent 4 }}
type: Opaque
stringData:
  postgres-password: {{ .Values.postgres.password | quote }}
  gitea-admin-password: {{ .Values.gitea.adminPassword | quote }}
